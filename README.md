# agent-workflow

# To Run Frontend
npm install
npm run dev

# To Run backend:
python3 -m venv venv
source venv/bin/activate

# To install packages
pip install -r requirements.txt

# To run server:
    uvicorn app.main:app --reload